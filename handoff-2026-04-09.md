안녕, 노바. 이전 Cowork 세션에서 이어받는 핸드오프야. 아래 내용을 읽고, 마지막 세션 시작 체크리스트 순서대로 바로 실행해줘. 확인 묻지 말고 진행해.

---

## 1. 이전 세션 완료 작업

### 1-1. dev-plan v2 → v3 업데이트 및 커밋
- **파일**: `cross-claude-dev-plan.md` (cross-claude-mcp 레포 + cross-claude-discord-bridge 레포 양쪽 존재)
- **v2 변경**: Claude Code 코드 리뷰 8개 지적사항 반영
  - `webhook으로 깨움` 아키텍처 오류 수정 (웹훅은 Discord 단방향 POST, Claude 세션 깨울 수 없음)
  - `includes()` → 단어 경계 정규식 (`escapeRegex` + negative lookahead/lookbehind)
  - pg_dump `~/.pgpass` 필수 명시 (ps aux 패스워드 노출 방지)
  - 1순위에 MCP_API_KEY 로테이션(1-D), /health 모니터링(1-E) 추가
  - 인스턴스 명칭 규칙 `{이름}-{Claude이름}[-{용도}]` 형식 통일
- **v3 변경**: Claude Code가 추가 작성
  - `1-F ⭐ 표준 시작 프롬프트 + CLAUDE.md 폴링 강제 룰` 추가 — "허브 등록" 한 마디로 register + wait_for_reply 진입
  - MCP_API_KEY 로테이션 절차 상세화 (서버 .env 2곳 + 클라이언트 PC별 + Cowork URL)
  - Bridge /health를 직접 추가 대신 `/api/instances`의 `discord-bridge` last_seen 간접 체크로 변경
  - 명칭 규칙 표 정정 (mark-nova-hq, mark-nova-cowork, casey-{claudename}-{용도} 예시 추가)
- **커밋**:
  - `kbc-devops/cross-claude-mcp` `reload-window` 브랜치: `021e77f` "docs: CLAUDE.md cross-claude rules + dev-plan v2"
  - `kbc-devops/cross-claude-discord-bridge` `main`: `5d7ad59` "docs: cross-claude-dev-plan v2"
  - toy01 bridge 레포: `5d7ad59` → Claude Code가 추가로 v3 업데이트 (로컬 파일 기준)

### 1-2. kbc-devops/cross-claude-mcp 레포 신규 생성
- **이전 상태**: 로컬 `C:\Users\heung\kbc-workspace\cross-claude-mcp`의 git remote가 `rblank9/cross-claude-mcp` (외부 오픈소스, push 권한 없음)
- **조치**:
  1. GitHub API로 `kbc-devops/cross-claude-mcp` 레포 생성 (PAT: `/home/mark/repos/kbc-infra/.secret` 내 `GITHUB_PAT`)
  2. 로컬 remote URL 변경: `git remote set-url origin https://github.com/kbc-devops/cross-claude-mcp.git`
  3. `git push origin --all` → main + reload-window 브랜치 모두 push
  4. CLAUDE.md + cross-claude-dev-plan.md 커밋/push (commit `021e77f`)
- **현재 로컬 브랜치**: `reload-window` (main과 별도 존재)
- **주의**: `.git/index.lock` 파일이 VS Code에 의해 잠금 걸려 있을 수 있음 → `del C:\Users\heung\kbc-workspace\cross-claude-mcp\.git\index.lock` 로 제거

### 1-3. cross-claude 시스템 업그레이드 현황 파악 및 Discord 공지
- toy01 `cross-claude-mcp` 레포 최근 커밋 (Claude Code 구현 완료):
  - `7e0c5de` fix(deploy-webhook): /webhook/deploy를 mcpAuthRouter 앞으로 이동
  - `4b18878` feat(3-A/3-B/3-C): 메시지 아카이브 + stale 인스턴스 자동 삭제 + shared_data TTL
  - `e6cbe76` fix(db): unread_mentions SCHEMA_SQL 누락 + SQLite webhook_url binding 수정
  - `034bcab` feat(2-A/2-C): unread_mentions 큐 + 단어 경계 멘션 감지
  - `415914c` feat: register 도구에 webhook_url 파라미터 직접 등록 지원
- toy01 `cross-claude-discord-bridge` 최근 커밋:
  - `3f7913b` feat(2-D): webhook config poll interval 10분 → 30초
- `#cross-ai-cowork` 채널 메시지 #185에 공지 전송 완료

### 1-4. nova-msla-server 미응답 원인 분석
- **증상**: 메시지 #199 (상세 배포 보고) 이후 #202~#204 3회 호출 무응답
- **원인**: 긴 응답 생성 후 wait_for_reply 폴링 루프 재진입 실패 (또는 컨텍스트 과부하)
- **해결**: Mark가 직접 해당 Claude Code 창에서 폴링 재진입 지시 필요. 근본 해결은 1-F (CLAUDE.md 폴링 강제 룰) 구현

---

## 2. 현재 서버/레포 상태

### toy01 서버 (cross.marksun.me)
| 프로세스 | 포트 | 버전 | 상태 |
|----------|------|------|------|
| cross-claude | 3500 | 2.0.0 | ✅ online (PM2 id: 8) |
| cross-claude-bridge | — | 1.0.0 | ✅ online (PM2 id: 7) |

- **MCP 엔드포인트**: `https://cross.marksun.me/mcp`
- **API 엔드포인트**: `https://cross.marksun.me/api`
- **배포 웹훅**: `POST https://cross.marksun.me/webhook/deploy` (HMAC-SHA256 검증)
- **DB**: PostgreSQL, `postgresql://crossclaude:***@127.0.0.1:5432/crossclaude`
- **레포 경로**: `/home/mark/repos/cross-claude-mcp`, `/home/mark/repos/cross-claude-discord-bridge`

### GitHub 레포
| 레포 | 브랜치 | 최신 커밋 |
|------|--------|----------|
| kbc-devops/cross-claude-mcp | main, reload-window | `021e77f` |
| kbc-devops/cross-claude-discord-bridge | main | `5d7ad59` |
| rblank9/cross-claude-mcp | main | `7e0c5de` (원본 오픈소스) |

### 로컬 (Windows)
- **경로**: `C:\Users\heung\kbc-workspace\cross-claude-mcp`
- **현재 브랜치**: `reload-window`
- **remote**: `https://github.com/kbc-devops/cross-claude-mcp.git`
- **bridge 레포 로컬 클론**: ❌ 없음 (toy01에만 있음)

### cross-claude 인스턴스 현황
- 모든 인스턴스 현재 `offline` (last_seen 기준)
- 가장 최근: `discord-bridge` (13:41 UTC), `mark-nova-hub` (11:13 UTC)
- nova-msla-server: 13:21 이후 무응답 → Mark가 직접 재가동 필요

---

## 3. 다음 작업 목록

### 🔴 즉시 (1순위) — Mark 직접 필요

| # | 작업 | 담당 | 비고 |
|---|------|------|------|
| 1-A | toy01 DB 백업 cron 설정 | **Mark** | `~/.pgpass` 먼저 설정, `rclone` 설치 여부 확인 필요 |
| 1-B | 백업 복원 테스트 1회 | **Mark** | pg_dump → 테스트 DB 복원 확인 |
| 1-D | MCP_API_KEY 로테이션 | **Mark** | 서버 .env 2곳 + PM2 재시작 + 모든 클라이언트 PC 재등록 + Cowork URL 갱신 |
| 1-E | UptimeRobot에 /health 등록 | **Mark** | `GET https://cross.marksun.me/health` + discord-bridge last_seen 모니터링 스크립트 |

### 🟡 이번 주 (1순위 Claude 작업)

| # | 작업 | 담당 | 비고 |
|---|------|------|------|
| 1-C | 복합 인덱스 추가 | **Claude** | `CREATE INDEX CONCURRENTLY idx_messages_channel_id_desc ON messages (channel, id DESC);` — 백업 완료 후 진행 |
| 1-F | CLAUDE.md 폴링 강제 룰 + 약어사전 추가 | **Claude** | `C:\Users\heung\kbc-workspace\CLAUDE.md` 수정 후 커밋 |

### 🟢 이번 주 (2순위)

| # | 작업 | 담당 | 비고 |
|---|------|------|------|
| 2-B | bridge.mjs 멘션 감지 로직 구현 | **Claude** | `detectMentions()` + `escapeRegex()` 추가 |
| 2-C | server.mjs register/check_messages에 unread_mentions 반환 | **Claude** | delivered_at 업데이트 포함 |
| 2-D | register 시 웹훅 즉시 리로드 | **Claude** | 이미 30초 리로드로 완화됐으나 즉시 반영 구현 |

---

## 4. 세션 시작 체크리스트

> 이 파일을 새 세션에서 받으면 아래 순서대로 즉시 실행. 중간에 묻지 말 것.

### Step 1: 로컬 레포 pull
```bash
# Windows cmd로 실행
set PATH=C:\Program Files\Git\cmd;%PATH%
cd C:\Users\heung\kbc-workspace\cross-claude-mcp
git pull origin reload-window
git status
```

### Step 2: toy01 서버 상태 확인 (sleepfriend-server MCP)
```bash
# server: toy01
pm2 list --no-color | grep -E "cross-claude|Name"
curl -s http://localhost:3500/health
```

### Step 3: cross-claude 채널 최신 메시지 확인 (cross-claude MCP)
```
check_messages channel=cross-ai-cowork, after_id=204
```

### Step 4: 인스턴스 등록 및 폴링 진입
```
register:
  instance_id: mark-nova-hq
  description: 마크의 Cowork 메인 세션
  webhook_url: https://discord.com/api/webhooks/1491647690113552465/lovFWWXZMKkgoyfD8h6iuFoPWOjhxXmRqsenNcv4nRhwTo_KgA5i3Wsb5osHLpKJgw3S

등록 후 즉시 wait_for_reply 폴링 진입:
  channel: cross-ai-cowork
  after_id: (마지막 수신 메시지 ID)
  timeout: 무한 반복 (timeout 리턴 시 즉시 재호출)
```

### Step 5: 다음 작업 판단
- 미응답 멘션 있으면 먼저 처리
- 없으면 1-C (인덱스 추가) 또는 1-F (CLAUDE.md 폴링 룰) 중 Mark 지시 따름

---

## 5. 참고: 주요 경로 및 키

| 항목 | 값 |
|------|-----|
| toy01 cross-claude 레포 | `/home/mark/repos/cross-claude-mcp` |
| toy01 bridge 레포 | `/home/mark/repos/cross-claude-discord-bridge` |
| 로컬 cross-claude 레포 | `C:\Users\heung\kbc-workspace\cross-claude-mcp` |
| MCP 엔드포인트 | `https://cross.marksun.me/mcp` |
| 배포 웹훅 | `POST https://cross.marksun.me/webhook/deploy` |
| 시크릿 파일 | `/home/mark/repos/kbc-infra/.secret` |
| mark-nova-hq 웹훅 | `https://discord.com/api/webhooks/1491647690113552465/lovFWWXZMKkgoyfD8h6iuFoPWOjhxXmRqsenNcv4nRhwTo_KgA5i3Wsb5osHLpKJgw3S` |
| mark-nova-hub 웹훅 | `https://discord.com/api/webhooks/1491647643359903794/2j01bAe40OrdzSr4x4jiIbIgfXjWts4NXqC4ohxiNx_Bg4oFFQ1c1j28j_EtGWC_dg61` |
| Discord 채널 | `#cross-ai-cowork` |
| 마지막 메시지 ID | `#204` |

---

> 작성: mark-nova-hq (Cowork)  
> 세션 날짜: 2026-04-09  
> 다음 핸드오프 파일: `handoff-2026-04-10.md` (다음 세션 종료 시 생성)
